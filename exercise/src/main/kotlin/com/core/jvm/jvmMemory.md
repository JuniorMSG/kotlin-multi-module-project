
# 🧠 JVM 메모리 구조 완벽 가이드

## 📊 **전체 구조 개요**

```
┌─────────────────────────────────────────────────────────────────┐
│                         JVM 메모리                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐  ┌──────────────────────────────────┐  │
│  │   Method Area      │  │          Heap                     │  │
│  │  (메서드 영역)      │  │         (힙)                      │  │
│  │  ────────────────  │  │  ──────────────────────────────  │  │
│  │  • 클래스 메타데이터 │  │  • 객체 인스턴스                │  │
│  │  • Static 변수      │  │  • 배열                          │  │
│  │  • 상수 풀          │  │  • 인스턴스 변수                 │  │
│  │  • 메서드 코드      │  │                                  │  │
│  │                    │  │  [Young Generation]              │  │
│  │  [모든 스레드 공유] │  │  [Old Generation]                │  │
│  └────────────────────┘  │  [Permanent/Metaspace]           │  │
│                          │                                  │  │
│                          │  [모든 스레드 공유]               │  │
│                          └──────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Thread 별 메모리 (스레드마다 독립)           │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                           │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  │  │
│  │  │   Stack     │  │   PC Register │  │ Native Method  │  │  │
│  │  │  (스택)      │  │  (프로그램     │  │    Stack       │  │  │
│  │  │             │  │   카운터)      │  │  (네이티브     │  │  │
│  │  │ • 메서드 호출│  │               │  │   메서드 스택)  │  │  │
│  │  │ • 지역 변수  │  │ • 현재 실행중인│  │                │  │  │
│  │  │ • 매개변수   │  │   명령어 주소  │  │ • JNI 호출     │  │  │
│  │  │ • 리턴 주소  │  │               │  │ • Native 코드  │  │  │
│  │  └─────────────┘  └──────────────┘  └────────────────┘  │  │
│  │                                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 **1. Method Area (메서드 영역)**

### 📝 **저장되는 것들**

````kotlin
class HelloCore {
	companion object {
		const val CONSTANT = "상수"  // ← Method Area
		var staticVar = "정적변수"   // ← Method Area
		
		fun staticMethod() {}      // ← 메서드 코드: Method Area
	}
	
	fun instanceMethod() {}        // ← 메서드 코드: Method Area
}
````

### 🗂️ **Method Area 내부 구조**

```
Method Area
├─ 클래스 메타데이터
│  ├─ 클래스 이름: "HelloCore"
│  ├─ 부모 클래스: "Object"
│  ├─ 인터페이스 목록
│  ├─ 접근 제어자: public
│  └─ 필드 정보
│     ├─ staticVar: String (static)
│     └─ CONSTANT: String (static final)
│
├─ 메서드 정보
│  ├─ staticMethod()
│  │  ├─ 바이트코드
│  │  ├─ 매개변수 정보
│  │  └─ 리턴 타입
│  └─ instanceMethod()
│     ├─ 바이트코드
│     └─ ...
│
├─ Runtime Constant Pool (상수 풀)
│  ├─ 문자열 리터럴: "상수", "정적변수"
│  ├─ 클래스/메서드 참조
│  └─ 숫자 상수
│
└─ Static 변수 저장소
   ├─ HelloCore.Companion.INSTANCE → 0x2000 (Heap 주소)
   └─ HelloCore.Companion.staticVar → "정적변수"
```

### 💡 **특징**

- ✅ **모든 스레드가 공유**
- ✅ **JVM 시작 시 생성**
- ✅ **클래스 로딩 시 데이터 추가**
- ⚠️ **Java 8 이전**: PermGen (고정 크기)
- ✅ **Java 8 이후**: Metaspace (동적 크기)

---

## 🏗️ **2. Heap (힙)**

### 📝 **저장되는 것들**

````kotlin
// 모든 객체 인스턴스는 Heap에 저장됨
val core = HelloCore()              // ← Heap
val list = mutableListOf(1, 2, 3)   // ← Heap
val array = arrayOf("a", "b")       // ← Heap
val lambda = { x: Int -> x * 2 }    // ← Heap (람다도 객체!)

data class User(val name: String)   // name도 Heap에 저장

val user = User("Monica")           // ← Heap
````

### 🗂️ **Heap 내부 구조 (Generational GC)**

```
Heap
│
├─ Young Generation (젊은 세대)
│  ├─ Eden Space
│  │  └─ 새로 생성된 객체들
│  │     ├─ HelloCore@0x1000
│  │     ├─ String@0x1010
│  │     └─ User@0x1020
│  │
│  ├─ Survivor 0 (S0)
│  │  └─ Minor GC에서 살아남은 객체
│  │
│  └─ Survivor 1 (S1)
│     └─ Minor GC에서 살아남은 객체
│
├─ Old Generation (늙은 세대)
│  └─ 오래 살아남은 객체들
│     ├─ Singleton 객체
│     ├─ 캐시 데이터
│     └─ 장수 객체
│
└─ Metaspace (Java 8+) / PermGen (Java 7-)
   └─ 클래스 메타데이터 (Method Area의 일부)
```

### 🔄 **객체 생명주기**

```
1. 객체 생성
   ↓
   Eden Space에 할당
   
2. Minor GC 발생
   ↓
   살아남은 객체 → Survivor 0
   
3. 또 Minor GC 발생
   ↓
   살아남은 객체 → Survivor 1
   
4. 여러 번 살아남음 (Age 증가)
   ↓
   Old Generation으로 승격 (Promotion)
   
5. Old Generation 가득 참
   ↓
   Major GC (Full GC) 발생
```

### 💡 **특징**

- ✅ **모든 스레드가 공유**
- ✅ **동적 메모리 할당**
- ✅ **GC(Garbage Collector)가 관리**
- ⚠️ **메모리 부족 시**: `OutOfMemoryError`

---

## 📚 **3. Stack (스택)**

### 📝 **저장되는 것들**

````kotlin
fun main() {                        // ← Stack Frame 1
	val x = 10                      // ← 지역 변수: Stack
	val core = HelloCore()          // ← 참조 변수: Stack (객체는 Heap)
	
	core.calculate(x)               // ← Stack Frame 2 생성
}

fun calculate(num: Int): Int {      // ← Stack Frame 2
	val result = num * 2            // ← 지역 변수: Stack
	return result                   // ← 리턴 후 Frame 제거
}
````

### 🗂️ **Stack 내부 구조**

```
Thread-1 Stack
│
├─ Stack Frame 3 (현재 실행 중)
│  ├─ Local Variables (지역 변수 테이블)
│  │  ├─ [0] this = 0x1000 (Heap 주소)
│  │  ├─ [1] result = 20
│  │  └─ [2] temp = "hello"
│  │
│  ├─ Operand Stack (피연산자 스택)
│  │  ├─ 20
│  │  └─ 10
│  │
│  └─ Frame Data
│     ├─ Return Address: 0x4A3C
│     ├─ Exception Table
│     └─ Constant Pool Reference
│
├─ Stack Frame 2
│  ├─ Local Variables
│  │  └─ [0] num = 10
│  └─ ...
│
└─ Stack Frame 1 (main)
   ├─ Local Variables
   │  ├─ [0] x = 10
   │  └─ [1] core = 0x1000 (Heap 주소 참조)
   └─ ...
```

### 🔍 **Stack Frame 상세**

````kotlin
fun example() {
	val a = 10          // ← Local Variables[0]
	val b = 20          // ← Local Variables[1]
	val sum = a + b     // ← Operand Stack 사용
	
	// 내부 동작:
	// 1. ILOAD 0      → a를 Operand Stack에 push
	// 2. ILOAD 1      → b를 Operand Stack에 push
	// 3. IADD         → pop 두 개, 더하기, push 결과
	// 4. ISTORE 2     → sum에 저장
}
````

### 💡 **특징**

- ✅ **각 스레드마다 독립적**
- ✅ **LIFO (Last In First Out) 구조**
- ✅ **메서드 호출마다 Frame 생성**
- ✅ **메서드 종료 시 Frame 제거**
- ⚠️ **스택 오버플로우**: `StackOverflowError`

---

## 🎮 **4. PC Register (Program Counter)**

### 📝 **역할**

```
Thread-1 PC Register
├─ 현재 실행 중인 바이트코드 주소: 0x4A3C
└─ 다음 실행할 명령어 위치 추적

Thread-2 PC Register
├─ 현재 실행 중인 바이트코드 주소: 0x5B2D
└─ (Thread-1과 독립적)
```

### 💡 **특징**

- ✅ **각 스레드마다 독립적**
- ✅ **매우 작은 메모리 공간**
- ✅ **CPU의 PC 레지스터와 유사**

---

## 🔧 **5. Native Method Stack**

### 📝 **역할**

````kotlin
// JNI를 통한 Native 메서드 호출
external fun nativeMethod(): Int    // ← C/C++ 코드 호출

// 내부적으로 Native Method Stack 사용
System.currentTimeMillis()          // ← Native 메서드
System.arraycopy()                  // ← Native 메서드
````

### 💡 **특징**

- ✅ **각 스레드마다 독립적**
- ✅ **JNI 호출 시 사용**
- ✅ **C/C++ 스택과 유사**

---

## 🎯 **실전 예제: 메모리 흐름 추적**

````kotlin
class User(val name: String) {
	companion object {
		var count = 0  // ← Method Area
		
		fun create(name: String): User {
			count++
			return User(name)
		}
	}
}

fun main() {
	val user = User.create("Monica")
	println(user.name)
}
````

### 📊 **메모리 배치**

```
┌─────────────────────────────────────────────────────────────┐
│ Method Area                                                  │
├─────────────────────────────────────────────────────────────┤
│ User 클래스 메타데이터                                        │
│ ├─ 클래스 이름: "User"                                       │
│ ├─ 필드: name (instance)                                    │
│ ├─ Static 변수: count = 1                                   │
│ └─ Companion.INSTANCE → 0x2000 (Heap 주소)                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Heap                                                         │
├─────────────────────────────────────────────────────────────┤
│ 0x2000: User$Companion@2000                                 │
│         └─ (싱글톤 객체)                                     │
│                                                              │
│ 0x3000: User@3000                                           │
│         └─ name → 0x4000                                    │
│                                                              │
│ 0x4000: String@4000                                         │
│         └─ value = ['M', 'o', 'n', 'i', 'c', 'a']          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Stack (Thread-main)                                          │
├─────────────────────────────────────────────────────────────┤
│ Frame: main()                                                │
│ ├─ Local Variables                                          │
│ │  └─ [0] user = 0x3000 (Heap 주소)                        │
│ └─ Operand Stack                                            │
│    └─ (비어있음)                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚨 **메모리 관련 에러**

### 1️⃣ **StackOverflowError**

````kotlin
fun recursive() {
	recursive()  // ← 무한 재귀
}

// Stack Frame이 계속 쌓여서 Stack 공간 초과
````

### 2️⃣ **OutOfMemoryError: Java heap space**

````kotlin
val list = mutableListOf<ByteArray>()
while (true) {
	list.add(ByteArray(1024 * 1024))  // ← Heap 메모리 고갈
}
````

### 3️⃣ **OutOfMemoryError: Metaspace**

````kotlin
// 동적으로 클래스를 무한정 생성
while (true) {
	val classLoader = CustomClassLoader()
	classLoader.loadClass("DynamicClass")  // ← Metaspace 고갈
}
````

---

## 📊 **메모리 영역 비교표**

| 영역               | 공유 여부 | 크기    | GC 대상 | 주요 저장 내용          |
|------------------|-------|-------|-------|-------------------|
| **Method Area**  | ✅ 공유  | 동적    | ❌     | 클래스 메타데이터, static |
| **Heap**         | ✅ 공유  | 동적    | ✅     | 객체, 배열            |
| **Stack**        | ❌ 독립  | 고정    | ❌     | 지역 변수, 메서드 호출     |
| **PC Register**  | ❌ 독립  | 매우 작음 | ❌     | 현재 명령어 주소         |
| **Native Stack** | ❌ 독립  | 고정    | ❌     | Native 메서드 호출     |

---

## 🎯 **핵심 정리**

```
┌──────────────────────────────────────────────────────────┐
│  메모리 영역별 핵심 포인트                                │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  1. Method Area (메서드 영역)                             │
│     → 클래스 정보, static 변수, 메서드 코드               │
│     → 모든 스레드 공유                                    │
│     → JVM 시작 시 생성                                    │
│                                                           │
│  2. Heap (힙)                                            │
│     → 모든 객체 인스턴스                                  │
│     → GC가 관리                                           │
│     → 모든 스레드 공유                                    │
│                                                           │
│  3. Stack (스택)                                         │
│     → 지역 변수, 메서드 호출 정보                         │
│     → 스레드마다 독립적                                   │
│     → LIFO 구조                                          │
│                                                           │
│  4. PC Register                                          │
│     → 현재 실행 중인 명령어 주소                          │
│     → 스레드마다 독립적                                   │
│                                                           │
│  5. Native Method Stack                                  │
│     → JNI 호출 시 사용                                   │
│     → 스레드마다 독립적                                   │
│                                                           │
└──────────────────────────────────────────────────────────┘
```